import { ChapterNamePipe } from './chapter-name.pipe';

describe('ChapterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ChapterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
