import { RelationshipPipe } from './relationship.pipe';

describe('GetRelationshipPipe', () => {
  it('create an instance', () => {
    const pipe = new RelationshipPipe();
    expect(pipe).toBeTruthy();
  });
});
