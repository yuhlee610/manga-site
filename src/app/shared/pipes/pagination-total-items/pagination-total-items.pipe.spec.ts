import { PaginationTotalItemsPipe } from './pagination-total-items.pipe';

describe('PaginationTotalItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationTotalItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
