import { ChapterImagePipe } from './chapter-image.pipe';

describe('ChapterImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ChapterImagePipe();
    expect(pipe).toBeTruthy();
  });
});
