import { MangaTitlePipe } from '../manga-title.pipe';

describe('MangaTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new MangaTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
