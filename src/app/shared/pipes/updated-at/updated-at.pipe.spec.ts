import { UpdatedAtPipe } from './updated-at.pipe';

describe('UpdatedAtPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdatedAtPipe();
    expect(pipe).toBeTruthy();
  });
});
